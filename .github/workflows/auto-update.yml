name: Auto Update Stata Journal Database

on:
  # 每天00:00 UTC自动运行（北京时间08:00）
  schedule:
    - cron: '0 0 * * *'
  
  # 允许手动触发
  workflow_dispatch:

jobs:
  update-database:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Allow pushing changes
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Full history for Gitee sync
        token: ${{ secrets.GITHUB_TOKEN }}
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        
    - name: Install Python dependencies
      run: |
        pip install pandas requests beautifulsoup4 pyreadstat
        
    - name: Run update script
      run: |
        python auto_update.py
        
    - name: Check for changes
      id: changes
      run: |
        if git diff --quiet findsj.dta findsj_version.dta; then
          echo "changed=false" >> $GITHUB_OUTPUT
        else
          echo "changed=true" >> $GITHUB_OUTPUT
        fi
        
    - name: Commit and push if changed
      if: steps.changes.outputs.changed == 'true'
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git add findsj.dta findsj_version.dta
        git commit -m "Auto update: Stata Journal database updated on $(date +'%Y-%m-%d')"
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Create update summary
      if: steps.changes.outputs.changed == 'true'
      run: |
        echo "## 📊 Stata Journal 数据库已更新" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "更新时间: $(date +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "数据文件: \`findsj.dta\`, \`findsj_version.dta\`" >> $GITHUB_STEP_SUMMARY
    
    - name: Sync to Gitee
      if: steps.changes.outputs.changed == 'true'
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.GITEE_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
        chmod 600 ~/.ssh/id_ed25519
        ssh-keyscan gitee.com >> ~/.ssh/known_hosts
        git remote add gitee git@gitee.com:ChuChengWan/findsj.git || true
        git push gitee main:main --force

